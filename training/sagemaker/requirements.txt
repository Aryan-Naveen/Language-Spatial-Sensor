# Installed automatically by SageMaker (SourceCode.requirements) on top of the
# PyTorch 2.4.0 + Python 3.11 DLC, before train_lss_sagemaker.py runs.
#
# torch / torchvision come from the DLC base image.

# --- Core deep learning stack (already in DLC, pin upgrades) ---
# <5.0 on transformers: 5.x has breaking API changes and was not exercised
# by the local smoke tests. Bump deliberately after re-running the suite.
transformers>=4.45.0,<5.0
huggingface-hub>=0.24.0,<1.0
tokenizers

# --- Config management ---
hydra-core>=1.3.2
omegaconf>=2.3.0
# Optuna is driven directly from training/sagemaker/optuna_sweep.py — we do
# NOT use hydra-optuna-sweeper (pins optuna<3, incompatible with our usage).
optuna>=3.4.0

# --- Numerics / geometry ---
numpy>=1.24.0
scipy>=1.12.0
einops>=0.7.0
pillow
tqdm>=4.65.0
matplotlib

# --- Misc runtime ---
typing-extensions
rich>=13.0.0
termcolor>=2.4.0

# --- Sharded data streaming ---
# Reads tar shards built by scripts/build_shards.py. One S3 GET per shard
# (~1000 samples) instead of per sample — the difference between ~0.1 s/step
# and ~2.7 s/step on FastFile.
webdataset>=0.2.86
